# Core numerical dependency used by the official baseline and generated models.
numpy

# Parse and validate agent-requested PyPI requirement specifiers before any
# user-approved runtime installation. URLs, markers, and pip flags are rejected.
packaging>=24


# Legacy Builder/Strategist comparison runner providers.
# Anthropic provider (`LLM_PROVIDER=anthropic`).
anthropic

# OpenAI and all OpenAI-compatible providers:
# - OpenAI (`LLM_PROVIDER=openai`)
# - Groq (`LLM_PROVIDER=groq`)
# - Google Gemini OpenAI-compatible endpoint (`LLM_PROVIDER=gemini`)
# - Ollama OpenAI-compatible local endpoint (`LLM_PROVIDER=ollama`)
openai

# LangChain supplies the model adapter (init_chat_model), unified usage/cache
# accounting, Pydantic-derived tool schemas, with_structured_output for the
# closing reflection, and trim_messages as the primitive under the harness's
# phase-aware memory policy.
#
# Deliberately NOT used: LangGraph and create_agent (the harness owns the loop,
# its state machine, and its retry, all of which must stay auditable); the
# client's own max_retries (it would multiply billed calls underneath the cost
# gate); with_fallbacks (a silent model swap mid-run would invalidate the
# scientific comparison between experiments); and LangSmith tracing (all traces
# stay local).
langchain>=1.0,<2
langchain-openai>=1.0,<2

# No separate `groq` or `ollama` package is needed for the legacy runner; it
# uses their OpenAI-compatible endpoints.
