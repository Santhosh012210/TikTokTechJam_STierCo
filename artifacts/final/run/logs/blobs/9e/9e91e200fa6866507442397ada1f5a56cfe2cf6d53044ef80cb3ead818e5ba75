{
  "path": "/Users/sabithajayakumar/TikTokTechJam_STierCo/experiment_workspace/20260901_012832/trial_008/model.py",
  "offset": 6000,
  "chars_returned": 2168,
  "total_chars": 8168,
  "next_offset": null,
  "complete": true,
  "content": "on = nn.BCEWithLogitsLoss()\nprogress = load_train_progress(a.data_dir)\nif len(progress) != len(ytr): raise RuntimeError('progress targets are not aligned to training rows')\nxt = torch.from_numpy(Xtr.astype(np.int64, copy=False)); yt = torch.from_numpy(ytr)\npt = torch.from_numpy(progress[:, 0]); completed = torch.from_numpy(progress[:, 1])\nxv = torch.from_numpy(Xva.astype(np.int64, copy=False))\nprogress_weight = float(trial.get('progress_weight', 0.08))\nrng = np.random.default_rng(a.seed); batch_size = int(trial.get('batch_size', 4096))\nbest, best_state, bad = -1.0, None, 0\n\ndef predict(X):\n    model.eval(); result = []\n    with torch.no_grad():\n        for i in range(0, len(X), 65536): result.append(model(X[i:i + 65536])[0].cpu().numpy())\n    return np.concatenate(result)\n\nfor epoch in range(int(trial.get('epochs', 20))):\n    model.train()\n    order = rng.permutation(len(ytr))\n    for i in range(0, len(order), batch_size):\n        j = torch.from_numpy(order[i:i + batch_size])\n        opt.zero_grad(set_to_none=True)\n        logits, predicted_progress = model(xt[j])\n        residual = predicted_progress - pt[j]\n        watch_loss = torch.where(completed[j] > 0, torch.relu(-residual).square(), residual.square()).mean()\n        loss = criterion(logits, yt[j]) + progress_weight * watch_loss\n        loss.backward(); opt.step()\n    result = evaluate(uva, yva, predict(xv))\n    if result['primary'] > best + 1e-5:\n        best, bad = result['primary'], 0\n        best_state = {k: v.detach().clone() for k, v in model.state_dict().items()}\n    else:\n        bad += 1\n        if bad >= int(trial.get('patience', 4)): break\nif best_state is None: raise RuntimeError('DCN did not produce checkpoint')\nmodel.load_state_dict(best_state)\nvalid_scores = predict(xv)\nif a.prediction_path:\n    write_validation_predictions(a.prediction_path, splits['valid'], valid_scores)\n    print(json.dumps({'status': 'predictions_written', 'rows': len(valid_scores), 'best_primary': float(best)}))\nif a.submission_path:\n    def score_fn(X): return predict(torch.from_numpy(X.astype(np.int64, copy=False)))\n    write_hidden_submission(a.submission_path, splits, enc, score_fn)\n"
}