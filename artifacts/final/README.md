# Final submission

Place only the explicitly selected final model and its submission evidence here.
Expected files are `model.py`, `metrics.json`, `submission.csv`, and `final-report.md`.
Generate them with `./mle_agent/scripts/finalize.sh`; do not promote a peak trial by hand.
