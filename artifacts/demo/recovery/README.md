# Automatic recovery demo

A deterministic fake provider drives the real persistent Google ADK tool loop through a syntax failure, a runtime failure, and a successful repair. Run `./mle_agent/scripts/demo_recovery.sh` to regenerate the evidence.
