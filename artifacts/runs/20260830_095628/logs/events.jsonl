{"iteration": 0, "session_type": "builder", "timestamp": "2026-08-30T09:57:05.953207Z", "parent_iteration": null, "hypothesis": "FM baseline (k=16, pointwise BCE, Adam) — no change", "hypothesis_source": "harness_root", "target_component": "other", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_095628/trial_000/model.py", "code_diff": "--- baseline\n+++ trial_000/model.py\n@@ -0,0 +1,79 @@\n+\"\"\"FM baseline (k=16, pointwise BCE) - Run as-is to establish reference score.\"\"\"\n+import sys\n+sys.path.insert(0, r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/baseline_kuairand-starter-kit')\n+\n+import argparse, json, time, collections\n+import numpy as np\n+from data import load, encode, FIELDS\n+from evaluate import evaluate\n+\n+\n+def sigmoid(x):\n+    return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n+\n+\n+class FM:\n+    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=42):\n+        rng = np.random.default_rng(seed)\n+        self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n+        self.W = np.zeros(dim, dtype=np.float32)\n+        self.b = np.float32(0.0)\n+        self.lr, self.l2 = lr, l2\n+        self.mV = np.zeros_like(self.V); self.vV = np.zeros_like(self.V)\n+        self.mW = np.zeros_like(self.W); self.vW = np.zeros_like(self.W)\n+        self.t = 0\n+\n+    def logits(self, X):\n+        E = self.V[X]\n+        S = E.sum(1)\n+        inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n+        return self.b + self.W[X].sum(1) + inter, E, S\n+\n+    def step(self, X, y):\n+        B = len(y)\n+        z, E, S = self.logits(X)\n+        g = ((sigmoid(z) - y) / B).astype(np.float32)\n+        gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n+        np.add.at(gW, X, g[:, None])\n+        np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n+        gV += self.l2 * self.V; gW += self.l2 * self.W\n+        self.t += 1\n+        b1, b2, eps = 0.9, 0.999, 1e-8\n+        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n+            M *= b1; M += (1 - b1) * G\n+            Vv *= b2; Vv += (1 - b2) * (G * G)\n+            P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n+        self.b -= self.lr * g.sum()\n+\n+    def predict(self, X, bs=200_000):\n+        return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n+\n+\n+ap = argparse.ArgumentParser()\n+ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n+a = ap.parse_args()\n+\n+splits = load(a.data_dir)\n+enc, dim = encode(splits)\n+Xtr, ytr, _ = enc['train']\n+Xva, yva, uva = enc['valid']\n+\n+m = FM(dim)\n+rng = np.random.default_rng(42)\n+best, best_state, bad = -1, None, 0\n+for ep in range(40):\n+    idx = rng.permutation(len(ytr))\n+    for i in range(0, len(idx), 8192):\n+        m.step(Xtr[idx[i:i + 8192]], ytr[idx[i:i + 8192]])\n+    va = evaluate(uva, yva, m.predict(Xva))\n+    if va['primary'] > best + 1e-5:\n+        best, bad = va['primary'], 0\n+        best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+    else:\n+        bad += 1\n+        if bad >= 4:\n+            break\n+\n+m.V, m.W, m.b = best_state\n+result = evaluate(uva, yva, m.predict(Xva))\n+print(json.dumps({key: float(value) for key, value in result.items()}))", "status": "success", "internal_repairs": 0, "metrics": {"GAUC": 0.6674313545227051, "nDCG@5": 0.536278486251831, "primary": 0.6018549203872681, "users": 22377.0, "rows": 124909.0}, "delta_vs_baseline": 0.0002549203872680428, "pct_of_headroom": 0.10329027036792657, "is_new_best": true, "error": null, "tokens": {"input": 13986, "output": 2083}, "wall_seconds": 37.706674098968506, "human_intervention": false}
{"iteration": 1, "session_type": "builder", "timestamp": "2026-08-30T09:57:53.834421Z", "parent_iteration": 0, "hypothesis": "Implement BPR: Bayesian Personalized Ranking on top of the parent model and evaluate it. See the 'bpr' entry in the method corpus for the implementation details and the conditions under which it does not help.", "hypothesis_source": "corpus_seed_bpr", "target_component": "loss", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_095628/trial_001/model.py", "code_diff": "--- trial_000/model.py\n+++ trial_001/model.py\n@@ -1,19 +1,18 @@\n-\"\"\"FM baseline (k=16, pointwise BCE) - Run as-is to establish reference score.\"\"\"\n import sys\n sys.path.insert(0, r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/baseline_kuairand-starter-kit')\n-\n-import argparse, json, time, collections\n-import numpy as np\n from data import load, encode, FIELDS\n from evaluate import evaluate\n+import json, argparse\n \n+import numpy as np\n+import time\n \n-def sigmoid(x):\n+def sigmoid(x): \n     return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n \n-\n-class FM:\n-    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=42):\n+# ============== Factorization Machine with BPR ==============\n+class FM_BPR:\n+    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=0):\n         rng = np.random.default_rng(seed)\n         self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n         self.W = np.zeros(dim, dtype=np.float32)\n@@ -24,56 +23,137 @@\n         self.t = 0\n \n     def logits(self, X):\n-        E = self.V[X]\n-        S = E.sum(1)\n+        E = self.V[X]                                   # (B,F,k)\n+        S = E.sum(1)                                    # (B,k)\n         inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n         return self.b + self.W[X].sum(1) + inter, E, S\n \n-    def step(self, X, y):\n-        B = len(y)\n-        z, E, S = self.logits(X)\n-        g = ((sigmoid(z) - y) / B).astype(np.float32)\n-        gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n-        np.add.at(gW, X, g[:, None])\n-        np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n-        gV += self.l2 * self.V; gW += self.l2 * self.W\n+    def _step_bpr(self, X_pos, X_neg):\n+        \"\"\"BPR step: score(pos) > score(neg).\"\"\"\n+        B = len(X_pos)\n+        z_pos, E_pos, S_pos = self.logits(X_pos)\n+        z_neg, E_neg, S_neg = self.logits(X_neg)\n+        \n+        # BPR loss: log(sigmoid(score_pos - score_neg))\n+        # Gradient w.r.t. score_pos: sigmoid(score_neg - score_pos)\n+        # Gradient w.r.t. score_neg: -sigmoid(score_neg - score_pos) = sigmoid(score_pos - score_neg) - 1\n+        delta = z_pos - z_neg\n+        g_pos = -sigmoid(-delta) / B  # gradient for positive examples\n+        g_neg = sigmoid(-delta) / B - 1.0 / B  # gradient for negative examples\n+        \n+        g_pos = g_pos.astype(np.float32)\n+        g_neg = g_neg.astype(np.float32)\n+        \n+        gV = np.zeros_like(self.V)\n+        gW = np.zeros_like(self.W)\n+        \n+        # Gradients for positive examples\n+        np.add.at(gW, X_pos, g_pos[:, None])\n+        np.add.at(gV, X_pos, g_pos[:, None, None] * (S_pos[:, None, :] - E_pos))\n+        \n+        # Gradients for negative examples\n+        np.add.at(gW, X_neg, g_neg[:, None])\n+        np.add.at(gV, X_neg, g_neg[:, None, None] * (S_neg[:, None, :] - E_neg))\n+        \n+        # L2 regularization\n+        gV += self.l2 * self.V\n+        gW += self.l2 * self.W\n+        \n+        # Adam optimizer\n         self.t += 1\n         b1, b2, eps = 0.9, 0.999, 1e-8\n         for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n             M *= b1; M += (1 - b1) * G\n             Vv *= b2; Vv += (1 - b2) * (G * G)\n             P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n-        self.b -= self.lr * g.sum()\n+        self.b -= self.lr * (g_pos.sum() + g_neg.sum())\n+        \n+        return float(-np.mean(np.log(sigmoid(delta) + 1e-9)))\n \n     def predict(self, X, bs=200_000):\n         return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n \n+def run_fm_bpr(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, seed=0, verbose=True):\n+    \"\"\"Train FM with BPR loss.\"\"\"\n+    enc, dim = encode(splits)\n+    Xtr, ytr, utr = enc['train']\n+    Xva, yva, uva = enc['valid']\n+    \n+    m = FM_BPR(dim, k=k, lr=lr, seed=seed)\n+    rng = np.random.default_rng(seed)\n+    \n+    best, best_state, bad = -1, None, 0\n+    \n+    for ep in range(1, epochs + 1):\n+        # Group by user to create positive/negative pairs\n+        user_idx = {}\n+        for i, uid in enumerate(utr):\n+            if uid not in user_idx:\n+                user_idx[uid] = []\n+            user_idx[uid].append(i)\n+        \n+        # For each user, pair positive and negative items\n+        pairs = []\n+        for uid, indices in user_idx.items():\n+            pos_indices = [i for i in indices if ytr[i] > 0.5]\n+            neg_indices = [i for i in indices if ytr[i] < 0.5]\n+            \n+            if len(pos_indices) > 0 and len(neg_indices) > 0:\n+                for p_idx in pos_indices:\n+                    for n_idx in neg_indices:\n+                        pairs.append((p_idx, n_idx))\n+        \n+        if not pairs:\n+            # Fallback if no pairs available\n+            if verbose:\n+                print(f\"  epoch {ep:2d} | no pairs available\")\n+            continue\n+        \n+        # Shuffle pairs\n+        pair_indices = rng.permutation(len(pairs))\n+        losses = []\n+        t0 = time.time()\n+        \n+        for batch_start in range(0, len(pair_indices), bs):\n+            batch_end = min(batch_start + bs, len(pair_indices))\n+            batch_pairs = [pairs[pair_indices[i]] for i in range(batch_start, batch_end)]\n+            \n+            X_pos = np.array([Xtr[p] for p, _ in batch_pairs])\n+            X_neg = np.array([Xtr[n] for _, n in batch_pairs])\n+            \n+            loss = m._step_bpr(X_pos, X_neg)\n+            losses.append(loss)\n+        \n+        va = evaluate(uva, yva, m.predict(Xva))\n+        if verbose:\n+            print(f\"  epoch {ep:2d} | loss {np.mean(losses):.4f} | valid GAUC {va['GAUC']:.4f} \"\n+                  f\"nDCG@5 {va['nDCG@5']:.4f} primary {va['primary']:.4f} | {time.time()-t0:.1f}s\")\n+        \n+        if va['primary'] > best + 1e-5:\n+            best, bad = va['primary'], 0\n+            best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+        else:\n+            bad += 1\n+            if bad >= patience:\n+                if verbose: \n+                    print(f\"  early stop at epoch {ep}\")\n+                break\n+    \n+    if best_state is not None:\n+        m.V, m.W, m.b = best_state\n+    \n+    return evaluate(uva, yva, m.predict(Xva))\n \n-ap = argparse.ArgumentParser()\n-ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n-a = ap.parse_args()\n \n-splits = load(a.data_dir)\n-enc, dim = encode(splits)\n-Xtr, ytr, _ = enc['train']\n-Xva, yva, uva = enc['valid']\n-\n-m = FM(dim)\n-rng = np.random.default_rng(42)\n-best, best_state, bad = -1, None, 0\n-for ep in range(40):\n-    idx = rng.permutation(len(ytr))\n-    for i in range(0, len(idx), 8192):\n-        m.step(Xtr[idx[i:i + 8192]], ytr[idx[i:i + 8192]])\n-    va = evaluate(uva, yva, m.predict(Xva))\n-    if va['primary'] > best + 1e-5:\n-        best, bad = va['primary'], 0\n-        best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n-    else:\n-        bad += 1\n-        if bad >= 4:\n-            break\n-\n-m.V, m.W, m.b = best_state\n-result = evaluate(uva, yva, m.predict(Xva))\n-print(json.dumps({key: float(value) for key, value in result.items()}))\n+if __name__ == '__main__':\n+    ap = argparse.ArgumentParser()\n+    ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n+    a = ap.parse_args()\n+    \n+    splits = load(a.data_dir)\n+    \n+    # Train with BPR\n+    result = run_fm_bpr(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, seed=42, verbose=True)\n+    \n+    # Return validation results as JSON\n+    print(json.dumps({k: float(v) for k, v in result.items()}))", "status": "failed", "internal_repairs": 2, "metrics": null, "delta_vs_baseline": null, "pct_of_headroom": null, "is_new_best": false, "error": "REJECTED: test-set access detected in model.py. Violations: ['enc[\\'test\\'] access (pattern: enc\\\\s*\\\\[\\\\s*[\\'\"]test[\\'\"]\\\\s*\\\\])', 'subscript access to the test split']. Remove any access to splits['test'] or enc['test'] and retry.", "tokens": {"input": 66123, "output": 5949}, "wall_seconds": 47.85559105873108, "human_intervention": false}
