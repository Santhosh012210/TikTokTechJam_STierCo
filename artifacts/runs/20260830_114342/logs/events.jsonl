{"iteration": 0, "session_type": "builder", "timestamp": "2026-08-30T11:44:21.158418Z", "parent_iteration": null, "hypothesis": "FM baseline (k=16, pointwise BCE, Adam) — no change", "hypothesis_source": "harness_root", "target_component": "other", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_114342/trial_000/model.py", "code_diff": "--- baseline\n+++ trial_000/model.py\n@@ -0,0 +1,79 @@\n+\"\"\"Root candidate: FM baseline (k=16, pointwise BCE) adapted for harness contract.\"\"\"\n+import sys\n+sys.path.insert(0, r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/baseline_kuairand-starter-kit')\n+\n+import argparse, json, time, collections\n+import numpy as np\n+from data import load, encode, FIELDS\n+from evaluate import evaluate\n+\n+\n+def sigmoid(x):\n+    return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n+\n+\n+class FM:\n+    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=42):\n+        rng = np.random.default_rng(seed)\n+        self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n+        self.W = np.zeros(dim, dtype=np.float32)\n+        self.b = np.float32(0.0)\n+        self.lr, self.l2 = lr, l2\n+        self.mV = np.zeros_like(self.V); self.vV = np.zeros_like(self.V)\n+        self.mW = np.zeros_like(self.W); self.vW = np.zeros_like(self.W)\n+        self.t = 0\n+\n+    def logits(self, X):\n+        E = self.V[X]\n+        S = E.sum(1)\n+        inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n+        return self.b + self.W[X].sum(1) + inter, E, S\n+\n+    def step(self, X, y):\n+        B = len(y)\n+        z, E, S = self.logits(X)\n+        g = ((sigmoid(z) - y) / B).astype(np.float32)\n+        gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n+        np.add.at(gW, X, g[:, None])\n+        np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n+        gV += self.l2 * self.V; gW += self.l2 * self.W\n+        self.t += 1\n+        b1, b2, eps = 0.9, 0.999, 1e-8\n+        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n+            M *= b1; M += (1 - b1) * G\n+            Vv *= b2; Vv += (1 - b2) * (G * G)\n+            P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n+        self.b -= self.lr * g.sum()\n+\n+    def predict(self, X, bs=200_000):\n+        return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n+\n+\n+ap = argparse.ArgumentParser()\n+ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n+a = ap.parse_args()\n+\n+splits = load(a.data_dir)\n+enc, dim = encode(splits)\n+Xtr, ytr, _ = enc['train']\n+Xva, yva, uva = enc['valid']\n+\n+m = FM(dim)\n+rng = np.random.default_rng(42)\n+best, best_state, bad = -1, None, 0\n+for ep in range(40):\n+    idx = rng.permutation(len(ytr))\n+    for i in range(0, len(idx), 8192):\n+        m.step(Xtr[idx[i:i + 8192]], ytr[idx[i:i + 8192]])\n+    va = evaluate(uva, yva, m.predict(Xva))\n+    if va['primary'] > best + 1e-5:\n+        best, bad = va['primary'], 0\n+        best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+    else:\n+        bad += 1\n+        if bad >= 4:\n+            break\n+\n+m.V, m.W, m.b = best_state\n+result = evaluate(uva, yva, m.predict(Xva))\n+print(json.dumps({key: float(value) for key, value in result.items()}))", "status": "success", "internal_repairs": 0, "metrics": {"GAUC": 0.6674313545227051, "nDCG@5": 0.536278486251831, "primary": 0.6018549203872681, "users": 22377.0, "rows": 124909.0}, "delta_vs_baseline": 0.0002549203872680428, "pct_of_headroom": 0.10329027036792657, "is_new_best": true, "error": null, "tokens": {"input": 15545, "output": 1945}, "wall_seconds": 38.76062512397766, "human_intervention": false}
{"iteration": 1, "session_type": "builder", "timestamp": "2026-08-30T11:45:40.911453Z", "parent_iteration": 0, "hypothesis": "Implement BPR: Bayesian Personalized Ranking on top of the parent model and evaluate it. See the 'bpr' entry in the method corpus for the implementation details and the conditions under which it does not help.", "hypothesis_source": "corpus_seed_bpr", "target_component": "loss", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_114342/trial_001/model.py", "code_diff": "--- trial_000/model.py\n+++ trial_001/model.py\n@@ -1,19 +1,17 @@\n-\"\"\"Root candidate: FM baseline (k=16, pointwise BCE) adapted for harness contract.\"\"\"\n import sys\n sys.path.insert(0, r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/baseline_kuairand-starter-kit')\n-\n-import argparse, json, time, collections\n-import numpy as np\n from data import load, encode, FIELDS\n from evaluate import evaluate\n-\n-\n-def sigmoid(x):\n+import json, argparse\n+\n+import numpy as np\n+import time\n+\n+def sigmoid(x): \n     return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n \n-\n class FM:\n-    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=42):\n+    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=0):\n         rng = np.random.default_rng(seed)\n         self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n         self.W = np.zeros(dim, dtype=np.float32)\n@@ -24,15 +22,16 @@\n         self.t = 0\n \n     def logits(self, X):\n-        E = self.V[X]\n-        S = E.sum(1)\n+        E = self.V[X]                                   # (B,F,k)\n+        S = E.sum(1)                                    # (B,k)\n         inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n         return self.b + self.W[X].sum(1) + inter, E, S\n \n     def step(self, X, y):\n+        \"\"\"Pointwise BCE step (original FM)\"\"\"\n         B = len(y)\n         z, E, S = self.logits(X)\n-        g = ((sigmoid(z) - y) / B).astype(np.float32)\n+        g = ((sigmoid(z) - y) / B).astype(np.float32)    # (B,)\n         gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n         np.add.at(gW, X, g[:, None])\n         np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n@@ -44,36 +43,174 @@\n             Vv *= b2; Vv += (1 - b2) * (G * G)\n             P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n         self.b -= self.lr * g.sum()\n+        return float(-np.mean(y * np.log(sigmoid(z) + 1e-9) + (1 - y) * np.log(1 - sigmoid(z) + 1e-9)))\n+\n+    def step_bpr(self, X_pos, X_neg):\n+        \"\"\"BPR step: pairs of (positive, negative) items for the same user\"\"\"\n+        B = len(X_pos)\n+        z_pos, E_pos, S_pos = self.logits(X_pos)  # (B,)\n+        z_neg, E_neg, S_neg = self.logits(X_neg)  # (B,)\n+        \n+        # BPR loss: we want score(pos) > score(neg)\n+        # -ln(sigmoid(z_pos - z_neg))\n+        z_diff = z_pos - z_neg\n+        sig_diff = sigmoid(z_diff)\n+        \n+        # Gradient of -ln(sigmoid(z_diff)) w.r.t. z_diff is: -(1 - sigmoid(z_diff))\n+        g_diff = (sig_diff - 1.0) / B  # (B,)\n+        \n+        # Now backprop to both positive and negative samples\n+        # d(z_pos - z_neg)/dz_pos = 1, d(z_pos - z_neg)/dz_neg = -1\n+        g_pos = g_diff.astype(np.float32)   # positive gradient\n+        g_neg = -g_diff.astype(np.float32)  # negative gradient\n+        \n+        gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n+        \n+        # Accumulate gradients for positive samples\n+        np.add.at(gW, X_pos, g_pos[:, None])\n+        np.add.at(gV, X_pos, g_pos[:, None, None] * (S_pos[:, None, :] - E_pos))\n+        \n+        # Accumulate gradients for negative samples\n+        np.add.at(gW, X_neg, g_neg[:, None])\n+        np.add.at(gV, X_neg, g_neg[:, None, None] * (S_neg[:, None, :] - E_neg))\n+        \n+        gV += self.l2 * self.V; gW += self.l2 * self.W\n+        self.t += 1\n+        b1, b2, eps = 0.9, 0.999, 1e-8\n+        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n+            M *= b1; M += (1 - b1) * G\n+            Vv *= b2; Vv += (1 - b2) * (G * G)\n+            P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n+        self.b -= self.lr * (g_pos.sum() + g_neg.sum())\n+        \n+        loss = -np.mean(np.log(sigmoid(z_diff) + 1e-9))\n+        return float(loss)\n \n     def predict(self, X, bs=200_000):\n         return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n \n \n-ap = argparse.ArgumentParser()\n-ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n-a = ap.parse_args()\n-\n-splits = load(a.data_dir)\n-enc, dim = encode(splits)\n-Xtr, ytr, _ = enc['train']\n-Xva, yva, uva = enc['valid']\n-\n-m = FM(dim)\n-rng = np.random.default_rng(42)\n-best, best_state, bad = -1, None, 0\n-for ep in range(40):\n-    idx = rng.permutation(len(ytr))\n-    for i in range(0, len(idx), 8192):\n-        m.step(Xtr[idx[i:i + 8192]], ytr[idx[i:i + 8192]])\n-    va = evaluate(uva, yva, m.predict(Xva))\n-    if va['primary'] > best + 1e-5:\n-        best, bad = va['primary'], 0\n-        best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n-    else:\n-        bad += 1\n-        if bad >= 4:\n-            break\n-\n-m.V, m.W, m.b = best_state\n-result = evaluate(uva, yva, m.predict(Xva))\n-print(json.dumps({key: float(value) for key, value in result.items()}))\n+def create_bpr_pairs(Xtr, ytr, users_tr, rng, neg_samples_per_pos=1):\n+    \"\"\"\n+    Create BPR training pairs from training data.\n+    For each user, sample (positive, negative) item pairs.\n+    \n+    Returns:\n+    - X_pos: (num_pairs, num_fields) - feature vectors for positive items\n+    - X_neg: (num_pairs, num_fields) - feature vectors for negative items\n+    \"\"\"\n+    \n+    # Group training data by user\n+    user_to_indices = {}\n+    for idx in range(len(users_tr)):\n+        u = users_tr[idx]\n+        if u not in user_to_indices:\n+            user_to_indices[u] = []\n+        user_to_indices[u].append(idx)\n+    \n+    # Separate positives and negatives per user\n+    user_to_pos = {}\n+    user_to_neg = {}\n+    \n+    for u, indices in user_to_indices.items():\n+        pos_idx = [i for i in indices if ytr[i] > 0.5]\n+        neg_idx = [i for i in indices if ytr[i] < 0.5]\n+        \n+        if len(pos_idx) > 0 and len(neg_idx) > 0:\n+            user_to_pos[u] = pos_idx\n+            user_to_neg[u] = neg_idx\n+    \n+    # Create pairs: for each user, for each positive, sample negatives\n+    pairs_pos = []\n+    pairs_neg = []\n+    \n+    for u in user_to_pos.keys():\n+        pos_indices = user_to_pos[u]\n+        neg_indices = user_to_neg[u]\n+        \n+        for pos_idx in pos_indices:\n+            for _ in range(neg_samples_per_pos):\n+                neg_idx = rng.choice(neg_indices)\n+                pairs_pos.append(pos_idx)\n+                pairs_neg.append(neg_idx)\n+    \n+    X_pos = Xtr[np.array(pairs_pos, dtype=np.int32)]\n+    X_neg = Xtr[np.array(pairs_neg, dtype=np.int32)]\n+    \n+    return X_pos, X_neg\n+\n+\n+def run_fm_bpr(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, seed=0, \n+               bpr_start_epoch=1, neg_samples_per_pos=1, verbose=True):\n+    \"\"\"\n+    FM with BPR loss starting from a certain epoch.\n+    Before bpr_start_epoch, use pointwise BCE.\n+    From bpr_start_epoch onwards, use BPR pairwise loss.\n+    \"\"\"\n+    enc, dim = encode(splits)\n+    Xtr, ytr, utr = enc['train']\n+    Xva, yva, uva = enc['valid']\n+    \n+    m = FM(dim, k=k, lr=lr, seed=seed)\n+    rng = np.random.default_rng(seed)\n+    best, best_state, bad = -1, None, 0\n+    \n+    for ep in range(1, epochs + 1):\n+        t0 = time.time()\n+        \n+        if ep < bpr_start_epoch:\n+            # Standard pointwise BCE training\n+            idx = rng.permutation(len(ytr))\n+            losses = [m.step(Xtr[idx[i:i + bs]], ytr[idx[i:i + bs]]) \n+                     for i in range(0, len(idx), bs)]\n+        else:\n+            # BPR training\n+            X_pos, X_neg = create_bpr_pairs(Xtr, ytr, utr, rng, neg_samples_per_pos)\n+            \n+            if len(X_pos) == 0:\n+                # Fallback to BCE if no pairs\n+                idx = rng.permutation(len(ytr))\n+                losses = [m.step(Xtr[idx[i:i + bs]], ytr[idx[i:i + bs]]) \n+                         for i in range(0, len(idx), bs)]\n+            else:\n+                pair_idx = rng.permutation(len(X_pos))\n+                losses = [m.step_bpr(X_pos[pair_idx[i:i + bs]], X_neg[pair_idx[i:i + bs]])\n+                         for i in range(0, len(pair_idx), bs)]\n+        \n+        va = evaluate(uva, yva, m.predict(Xva))\n+        \n+        if verbose:\n+            print(f\"  epoch {ep:2d} | loss {np.mean(losses):.4f} | valid GAUC {va['GAUC']:.4f} \"\n+                  f\"nDCG@5 {va['nDCG@5']:.4f} primary {va['primary']:.4f} | {time.time()-t0:.1f}s\")\n+        \n+        if va['primary'] > best + 1e-5:\n+            best, bad = va['primary'], 0\n+            best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+        else:\n+            bad += 1\n+            if bad >= patience:\n+                if verbose: \n+                    print(f\"  early stop at epoch {ep}\")\n+                break\n+    \n+    m.V, m.W, m.b = best_state\n+    return evaluate(uva, yva, m.predict(Xva))\n+\n+\n+if __name__ == '__main__':\n+    ap = argparse.ArgumentParser()\n+    ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n+    a = ap.parse_args()\n+    \n+    print(f\"loading {a.data_dir} ...\")\n+    splits = load(a.data_dir)\n+    print({k_: len(v) for k_, v in splits.items()}, f\"fields={FIELDS}\")\n+    \n+    print(f\"\\n=== FM + BPR (seed=42) ===\")\n+    result = run_fm_bpr(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, \n+                       seed=42, bpr_start_epoch=1, neg_samples_per_pos=1, verbose=True)\n+    \n+    print(f\"\\n=== Final Validation Results ===\")\n+    print(f\"  GAUC {result['GAUC']:.4f} | nDCG@5 {result['nDCG@5']:.4f} | primary {result['primary']:.4f}\")\n+    \n+    print(json.dumps({k: float(v) for k, v in result.items()}))", "status": "success", "internal_repairs": 0, "metrics": {"GAUC": 0.6704425811767578, "nDCG@5": 0.5368787050247192, "primary": 0.6036606431007385, "users": 22377.0, "rows": 124909.0}, "delta_vs_baseline": 0.0020606431007385018, "pct_of_headroom": 0.8349445302830234, "is_new_best": true, "error": null, "tokens": {"input": 66022, "output": 4520}, "wall_seconds": 79.74221014976501, "human_intervention": false}
{"iteration": 2, "session_type": "builder", "timestamp": "2026-08-30T11:47:35.471475Z", "parent_iteration": 1, "hypothesis": "Implement Censored regression for watch time on top of the parent model and evaluate it. See the 'censored-regression' entry in the method corpus for the implementation details and the conditions under which it does not help.", "hypothesis_source": "corpus_seed_censored-regression", "target_component": "features", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_114342/trial_002/model.py", "code_diff": "--- trial_001/model.py\n+++ trial_002/model.py\n@@ -5,177 +5,208 @@\n import json, argparse\n \n import numpy as np\n+import collections\n import time\n \n def sigmoid(x): \n     return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n \n class FM:\n+    \"\"\"Factorization Machine with censored regression auxiliary task for watch time.\"\"\"\n+    \n     def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=0):\n         rng = np.random.default_rng(seed)\n         self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n         self.W = np.zeros(dim, dtype=np.float32)\n         self.b = np.float32(0.0)\n+        \n+        # Additional parameters for censored regression (watch time auxiliary task)\n+        self.V_watch = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n+        self.W_watch = np.zeros(dim, dtype=np.float32)\n+        self.b_watch = np.float32(0.0)\n+        \n         self.lr, self.l2 = lr, l2\n         self.mV = np.zeros_like(self.V); self.vV = np.zeros_like(self.V)\n         self.mW = np.zeros_like(self.W); self.vW = np.zeros_like(self.W)\n+        self.mV_watch = np.zeros_like(self.V_watch); self.vV_watch = np.zeros_like(self.V_watch)\n+        self.mW_watch = np.zeros_like(self.W_watch); self.vW_watch = np.zeros_like(self.W_watch)\n+        self.mb_watch = 0.0; self.vb_watch = 0.0\n         self.t = 0\n \n     def logits(self, X):\n+        \"\"\"Compute logits for binary classification (long_view).\"\"\"\n         E = self.V[X]                                   # (B,F,k)\n         S = E.sum(1)                                    # (B,k)\n         inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n         return self.b + self.W[X].sum(1) + inter, E, S\n \n-    def step(self, X, y):\n-        \"\"\"Pointwise BCE step (original FM)\"\"\"\n+    def logits_watch(self, X):\n+        \"\"\"Compute logits for watch time regression.\"\"\"\n+        E = self.V_watch[X]                             # (B,F,k)\n+        S = E.sum(1)                                    # (B,k)\n+        inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n+        return self.b_watch + self.W_watch[X].sum(1) + inter, E, S\n+\n+    def step(self, X, y, watch_time, watch_weight=0.05, verbose_idx=None):\n+        \"\"\"\n+        Training step with binary classification loss and censored regression auxiliary task.\n+        \n+        Args:\n+            X: feature matrix (B, F)\n+            y: binary labels for long_view (B,)\n+            watch_time: normalized watch time (B,)\n+            watch_weight: weight of watch time auxiliary loss\n+        \"\"\"\n         B = len(y)\n+        \n+        # Primary task: binary classification for long_view\n         z, E, S = self.logits(X)\n         g = ((sigmoid(z) - y) / B).astype(np.float32)    # (B,)\n-        gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n+        \n+        gV = np.zeros_like(self.V)\n+        gW = np.zeros_like(self.W)\n         np.add.at(gW, X, g[:, None])\n         np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n-        gV += self.l2 * self.V; gW += self.l2 * self.W\n+        gV += self.l2 * self.V\n+        gW += self.l2 * self.W\n+        \n+        # Auxiliary task: censored regression for watch time\n+        # Key insight: videos with long_view=1 have complete watch time information\n+        # Videos with long_view=0 have censored watch times (unknown true potential)\n+        z_watch, E_watch, S_watch = self.logits_watch(X)\n+        \n+        # Create censoring indicator: long_view=1 means uncensored\n+        uncensored = (y > 0.5).astype(np.float32)\n+        \n+        # Predicted watch times (bounded for numerical stability)\n+        watch_pred = np.clip(z_watch, -5, 5).astype(np.float32)\n+        watch_targets = np.clip(watch_time, 0, 1).astype(np.float32)\n+        \n+        # Censored regression loss:\n+        # For uncensored (long_view=1): use squared error\n+        # For censored (long_view=0): use only one-sided penalty when prediction > target\n+        residuals = watch_pred - watch_targets\n+        \n+        # Loss: symmetric for uncensored, asymmetric for censored\n+        loss_terms = np.where(\n+            uncensored > 0.5,\n+            residuals ** 2,  # Uncensored: squared error\n+            np.maximum(residuals, 0) ** 2  # Censored: only penalize overestimation\n+        )\n+        \n+        # Gradient: scale by whether we have residual\n+        grad_factor = np.where(\n+            uncensored > 0.5,\n+            2.0 * residuals,  # Uncensored: normal gradient\n+            2.0 * np.maximum(residuals, 0)  # Censored: gradient only if positive residual\n+        )\n+        \n+        g_watch = (watch_weight * grad_factor / max(B, 1)).astype(np.float32)\n+        \n+        gV_watch = np.zeros_like(self.V_watch)\n+        gW_watch = np.zeros_like(self.W_watch)\n+        np.add.at(gW_watch, X, g_watch[:, None])\n+        np.add.at(gV_watch, X, g_watch[:, None, None] * (S_watch[:, None, :] - E_watch))\n+        gV_watch += self.l2 * self.V_watch\n+        gW_watch += self.l2 * self.W_watch\n+        \n+        # Combine gradients\n+        gV += gV_watch\n+        gW += gW_watch\n+        \n+        # Adam optimizer\n         self.t += 1\n         b1, b2, eps = 0.9, 0.999, 1e-8\n-        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n-            M *= b1; M += (1 - b1) * G\n-            Vv *= b2; Vv += (1 - b2) * (G * G)\n+        \n+        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), \n+                            (self.W, gW, self.mW, self.vW),\n+                            (self.V_watch, gV_watch, self.mV_watch, self.vV_watch),\n+                            (self.W_watch, gW_watch, self.mW_watch, self.vW_watch)):\n+            M *= b1\n+            M += (1 - b1) * G\n+            Vv *= b2\n+            Vv += (1 - b2) * (G * G)\n             P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n+        \n         self.b -= self.lr * g.sum()\n-        return float(-np.mean(y * np.log(sigmoid(z) + 1e-9) + (1 - y) * np.log(1 - sigmoid(z) + 1e-9)))\n-\n-    def step_bpr(self, X_pos, X_neg):\n-        \"\"\"BPR step: pairs of (positive, negative) items for the same user\"\"\"\n-        B = len(X_pos)\n-        z_pos, E_pos, S_pos = self.logits(X_pos)  # (B,)\n-        z_neg, E_neg, S_neg = self.logits(X_neg)  # (B,)\n-        \n-        # BPR loss: we want score(pos) > score(neg)\n-        # -ln(sigmoid(z_pos - z_neg))\n-        z_diff = z_pos - z_neg\n-        sig_diff = sigmoid(z_diff)\n-        \n-        # Gradient of -ln(sigmoid(z_diff)) w.r.t. z_diff is: -(1 - sigmoid(z_diff))\n-        g_diff = (sig_diff - 1.0) / B  # (B,)\n-        \n-        # Now backprop to both positive and negative samples\n-        # d(z_pos - z_neg)/dz_pos = 1, d(z_pos - z_neg)/dz_neg = -1\n-        g_pos = g_diff.astype(np.float32)   # positive gradient\n-        g_neg = -g_diff.astype(np.float32)  # negative gradient\n-        \n-        gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n-        \n-        # Accumulate gradients for positive samples\n-        np.add.at(gW, X_pos, g_pos[:, None])\n-        np.add.at(gV, X_pos, g_pos[:, None, None] * (S_pos[:, None, :] - E_pos))\n-        \n-        # Accumulate gradients for negative samples\n-        np.add.at(gW, X_neg, g_neg[:, None])\n-        np.add.at(gV, X_neg, g_neg[:, None, None] * (S_neg[:, None, :] - E_neg))\n-        \n-        gV += self.l2 * self.V; gW += self.l2 * self.W\n-        self.t += 1\n-        b1, b2, eps = 0.9, 0.999, 1e-8\n-        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n-            M *= b1; M += (1 - b1) * G\n-            Vv *= b2; Vv += (1 - b2) * (G * G)\n-            P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n-        self.b -= self.lr * (g_pos.sum() + g_neg.sum())\n-        \n-        loss = -np.mean(np.log(sigmoid(z_diff) + 1e-9))\n-        return float(loss)\n+        \n+        # Update b_watch with Adam\n+        gb = g_watch.sum()\n+        self.mb_watch = b1 * self.mb_watch + (1 - b1) * gb\n+        self.vb_watch = b2 * self.vb_watch + (1 - b2) * (gb * gb)\n+        self.b_watch -= self.lr * (self.mb_watch / (1 - b1 ** self.t)) / (np.sqrt(self.vb_watch / (1 - b2 ** self.t)) + eps)\n+        \n+        # Return primary loss for monitoring\n+        bce_loss = float(-np.mean(y * np.log(sigmoid(z) + 1e-9) + (1 - y) * np.log(1 - sigmoid(z) + 1e-9)))\n+        return bce_loss\n \n     def predict(self, X, bs=200_000):\n+        \"\"\"Predict using primary task (long_view).\"\"\"\n         return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n \n-\n-def create_bpr_pairs(Xtr, ytr, users_tr, rng, neg_samples_per_pos=1):\n-    \"\"\"\n-    Create BPR training pairs from training data.\n-    For each user, sample (positive, negative) item pairs.\n-    \n-    Returns:\n-    - X_pos: (num_pairs, num_fields) - feature vectors for positive items\n-    - X_neg: (num_pairs, num_fields) - feature vectors for negative items\n-    \"\"\"\n-    \n-    # Group training data by user\n-    user_to_indices = {}\n-    for idx in range(len(users_tr)):\n-        u = users_tr[idx]\n-        if u not in user_to_indices:\n-            user_to_indices[u] = []\n-        user_to_indices[u].append(idx)\n-    \n-    # Separate positives and negatives per user\n-    user_to_pos = {}\n-    user_to_neg = {}\n-    \n-    for u, indices in user_to_indices.items():\n-        pos_idx = [i for i in indices if ytr[i] > 0.5]\n-        neg_idx = [i for i in indices if ytr[i] < 0.5]\n-        \n-        if len(pos_idx) > 0 and len(neg_idx) > 0:\n-            user_to_pos[u] = pos_idx\n-            user_to_neg[u] = neg_idx\n-    \n-    # Create pairs: for each user, for each positive, sample negatives\n-    pairs_pos = []\n-    pairs_neg = []\n-    \n-    for u in user_to_pos.keys():\n-        pos_indices = user_to_pos[u]\n-        neg_indices = user_to_neg[u]\n-        \n-        for pos_idx in pos_indices:\n-            for _ in range(neg_samples_per_pos):\n-                neg_idx = rng.choice(neg_indices)\n-                pairs_pos.append(pos_idx)\n-                pairs_neg.append(neg_idx)\n-    \n-    X_pos = Xtr[np.array(pairs_pos, dtype=np.int32)]\n-    X_neg = Xtr[np.array(pairs_neg, dtype=np.int32)]\n-    \n-    return X_pos, X_neg\n-\n-\n-def run_fm_bpr(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, seed=0, \n-               bpr_start_epoch=1, neg_samples_per_pos=1, verbose=True):\n-    \"\"\"\n-    FM with BPR loss starting from a certain epoch.\n-    Before bpr_start_epoch, use pointwise BCE.\n-    From bpr_start_epoch onwards, use BPR pairwise loss.\n+# Helper function to prepare watch time data\n+def prepare_watch_time_data(splits):\n+    \"\"\"\n+    Extract watch time from raw data.\n+    Returns dictionaries mapping split name to watch time arrays.\n+    \"\"\"\n+    watch_times = {}\n+    \n+    for split_name, data in splits.items():\n+        # Normalize watch times by dividing by max (from training data)\n+        watch_times_list = [x[5] for x in data]  # duration_ms is at index 5\n+        watch_times[split_name] = np.array(watch_times_list, dtype=np.float32)\n+    \n+    # Normalize by training max\n+    train_max = np.max(watch_times['train'])\n+    train_max = max(train_max, 1.0)  # Avoid division by zero\n+    \n+    for split_name in watch_times:\n+        watch_times[split_name] = watch_times[split_name] / train_max\n+    \n+    return watch_times\n+\n+def run_fm_censored(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, seed=0, watch_weight=0.05, verbose=True):\n+    \"\"\"\n+    Train FM with censored regression auxiliary task.\n+    \n+    Args:\n+        splits: raw data splits from load()\n+        k: embedding dimension\n+        lr: learning rate\n+        epochs: max epochs\n+        bs: batch size\n+        patience: early stopping patience\n+        seed: random seed\n+        watch_weight: weight of watch time auxiliary loss\n+        verbose: print progress\n     \"\"\"\n     enc, dim = encode(splits)\n     Xtr, ytr, utr = enc['train']\n     Xva, yva, uva = enc['valid']\n     \n+    # Prepare watch time data\n+    watch_times = prepare_watch_time_data(splits)\n+    watch_times_tr = watch_times['train']\n+    watch_times_va = watch_times['valid']\n+    \n     m = FM(dim, k=k, lr=lr, seed=seed)\n     rng = np.random.default_rng(seed)\n     best, best_state, bad = -1, None, 0\n     \n     for ep in range(1, epochs + 1):\n+        idx = rng.permutation(len(ytr))\n         t0 = time.time()\n-        \n-        if ep < bpr_start_epoch:\n-            # Standard pointwise BCE training\n-            idx = rng.permutation(len(ytr))\n-            losses = [m.step(Xtr[idx[i:i + bs]], ytr[idx[i:i + bs]]) \n-                     for i in range(0, len(idx), bs)]\n-        else:\n-            # BPR training\n-            X_pos, X_neg = create_bpr_pairs(Xtr, ytr, utr, rng, neg_samples_per_pos)\n+        losses = []\n+        \n+        for i in range(0, len(idx), bs):\n+            batch_idx = idx[i:i + bs]\n+            Xb = Xtr[batch_idx]\n+            yb = ytr[batch_idx]\n+            wb = watch_times_tr[batch_idx]\n             \n-            if len(X_pos) == 0:\n-                # Fallback to BCE if no pairs\n-                idx = rng.permutation(len(ytr))\n-                losses = [m.step(Xtr[idx[i:i + bs]], ytr[idx[i:i + bs]]) \n-                         for i in range(0, len(idx), bs)]\n-            else:\n-                pair_idx = rng.permutation(len(X_pos))\n-                losses = [m.step_bpr(X_pos[pair_idx[i:i + bs]], X_neg[pair_idx[i:i + bs]])\n-                         for i in range(0, len(pair_idx), bs)]\n+            loss = m.step(Xb, yb, wb, watch_weight=watch_weight)\n+            losses.append(loss)\n         \n         va = evaluate(uva, yva, m.predict(Xva))\n         \n@@ -185,7 +216,8 @@\n         \n         if va['primary'] > best + 1e-5:\n             best, bad = va['primary'], 0\n-            best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+            best_state = (m.V.copy(), m.W.copy(), np.float32(m.b),\n+                         m.V_watch.copy(), m.W_watch.copy(), np.float32(m.b_watch))\n         else:\n             bad += 1\n             if bad >= patience:\n@@ -193,9 +225,8 @@\n                     print(f\"  early stop at epoch {ep}\")\n                 break\n     \n-    m.V, m.W, m.b = best_state\n-    return evaluate(uva, yva, m.predict(Xva))\n-\n+    m.V, m.W, m.b, m.V_watch, m.W_watch, m.b_watch = best_state\n+    return {'valid': evaluate(uva, yva, m.predict(Xva))}\n \n if __name__ == '__main__':\n     ap = argparse.ArgumentParser()\n@@ -204,13 +235,12 @@\n     \n     print(f\"loading {a.data_dir} ...\")\n     splits = load(a.data_dir)\n-    print({k_: len(v) for k_, v in splits.items()}, f\"fields={FIELDS}\")\n-    \n-    print(f\"\\n=== FM + BPR (seed=42) ===\")\n-    result = run_fm_bpr(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, \n-                       seed=42, bpr_start_epoch=1, neg_samples_per_pos=1, verbose=True)\n-    \n-    print(f\"\\n=== Final Validation Results ===\")\n-    print(f\"  GAUC {result['GAUC']:.4f} | nDCG@5 {result['nDCG@5']:.4f} | primary {result['primary']:.4f}\")\n-    \n-    print(json.dumps({k: float(v) for k, v in result.items()}))\n+    print({k: len(v) for k, v in splits.items()}, f\"fields={FIELDS}\")\n+    \n+    print(\"\\n=== FM with Censored Regression (seed=42) ===\")\n+    res = run_fm_censored(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, \n+                          seed=42, watch_weight=0.05, verbose=True)\n+    \n+    r = res['valid']\n+    result = {'GAUC': float(r['GAUC']), 'nDCG@5': float(r['nDCG@5']), 'primary': float(r['primary'])}\n+    print(json.dumps(result))", "status": "success", "internal_repairs": 0, "metrics": {"GAUC": 0.6679130792617798, "nDCG@5": 0.5367118716239929, "primary": 0.602312445640564}, "delta_vs_baseline": 0.0007124456405639412, "pct_of_headroom": 0.28867327413449806, "is_new_best": false, "error": null, "tokens": {"input": 83234, "output": 8404}, "wall_seconds": 114.55675911903381, "human_intervention": false}
