{"iteration": 0, "session_type": "builder", "timestamp": "2026-08-30T09:22:42.797013Z", "parent_iteration": null, "hypothesis": "FM baseline (k=16, pointwise BCE, Adam) — no change", "hypothesis_source": "harness_root", "target_component": "other", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_092147/trial_000/model.py", "code_diff": "--- baseline\n+++ trial_000/model.py\n@@ -0,0 +1,80 @@\n+\"\"\"FM baseline (k=16, pointwise BCE) - reference implementation.\"\"\"\n+import sys\n+sys.path.insert(0, r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/baseline_kuairand-starter-kit')\n+from data import load, encode, FIELDS\n+from evaluate import evaluate\n+import json, argparse\n+\n+import time, collections\n+import numpy as np\n+\n+\n+def sigmoid(x):\n+    return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n+\n+\n+class FM:\n+    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=42):\n+        rng = np.random.default_rng(seed)\n+        self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n+        self.W = np.zeros(dim, dtype=np.float32)\n+        self.b = np.float32(0.0)\n+        self.lr, self.l2 = lr, l2\n+        self.mV = np.zeros_like(self.V); self.vV = np.zeros_like(self.V)\n+        self.mW = np.zeros_like(self.W); self.vW = np.zeros_like(self.W)\n+        self.t = 0\n+\n+    def logits(self, X):\n+        E = self.V[X]\n+        S = E.sum(1)\n+        inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n+        return self.b + self.W[X].sum(1) + inter, E, S\n+\n+    def step(self, X, y):\n+        B = len(y)\n+        z, E, S = self.logits(X)\n+        g = ((sigmoid(z) - y) / B).astype(np.float32)\n+        gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n+        np.add.at(gW, X, g[:, None])\n+        np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n+        gV += self.l2 * self.V; gW += self.l2 * self.W\n+        self.t += 1\n+        b1, b2, eps = 0.9, 0.999, 1e-8\n+        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n+            M *= b1; M += (1 - b1) * G\n+            Vv *= b2; Vv += (1 - b2) * (G * G)\n+            P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n+        self.b -= self.lr * g.sum()\n+\n+    def predict(self, X, bs=200_000):\n+        return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n+\n+\n+ap = argparse.ArgumentParser()\n+ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n+a = ap.parse_args()\n+\n+splits = load(a.data_dir)\n+enc, dim = encode(splits)\n+Xtr, ytr, _ = enc['train']\n+Xva, yva, uva = enc['valid']\n+\n+m = FM(dim)\n+rng = np.random.default_rng(42)\n+best, best_state, bad = -1, None, 0\n+for ep in range(40):\n+    idx = rng.permutation(len(ytr))\n+    for i in range(0, len(idx), 8192):\n+        m.step(Xtr[idx[i:i + 8192]], ytr[idx[i:i + 8192]])\n+    va = evaluate(uva, yva, m.predict(Xva))\n+    if va['primary'] > best + 1e-5:\n+        best, bad = va['primary'], 0\n+        best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+    else:\n+        bad += 1\n+        if bad >= 4:\n+            break\n+\n+m.V, m.W, m.b = best_state\n+result = evaluate(uva, yva, m.predict(Xva))\n+print(json.dumps({key: float(value) for key, value in result.items()}))", "status": "success", "internal_repairs": 0, "metrics": {"GAUC": 0.6674313545227051, "nDCG@5": 0.536278486251831, "primary": 0.6018549203872681, "users": 22377.0, "rows": 124909.0}, "delta_vs_baseline": 0.0002549203872680428, "pct_of_headroom": 0.10329027036792657, "is_new_best": true, "error": null, "tokens": {"input": 38813, "output": 3871}, "wall_seconds": 55.32133197784424, "human_intervention": false}
{"iteration": 1, "session_type": "builder", "timestamp": "2026-08-30T09:23:53.502942Z", "parent_iteration": 0, "hypothesis": "Implement BPR: Bayesian Personalized Ranking on top of the parent model and evaluate it. See the 'bpr' entry in the method corpus for the implementation details and the conditions under which it does not help.", "hypothesis_source": "corpus_seed_bpr", "target_component": "loss", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_092147/trial_001/model.py", "code_diff": "--- trial_000/model.py\n+++ trial_001/model.py\n@@ -1,20 +1,18 @@\n-\"\"\"FM baseline (k=16, pointwise BCE) - reference implementation.\"\"\"\n import sys\n sys.path.insert(0, r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/baseline_kuairand-starter-kit')\n from data import load, encode, FIELDS\n from evaluate import evaluate\n import json, argparse\n \n-import time, collections\n import numpy as np\n+import time\n+import collections\n \n-\n-def sigmoid(x):\n+def sigmoid(x): \n     return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n \n-\n class FM:\n-    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=42):\n+    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=0):\n         rng = np.random.default_rng(seed)\n         self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n         self.W = np.zeros(dim, dtype=np.float32)\n@@ -25,56 +23,175 @@\n         self.t = 0\n \n     def logits(self, X):\n-        E = self.V[X]\n-        S = E.sum(1)\n+        E = self.V[X]                                   # (B,F,k)\n+        S = E.sum(1)                                    # (B,k)\n         inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n         return self.b + self.W[X].sum(1) + inter, E, S\n \n-    def step(self, X, y):\n-        B = len(y)\n-        z, E, S = self.logits(X)\n-        g = ((sigmoid(z) - y) / B).astype(np.float32)\n+    def step_bpr(self, X_pos, X_neg):\n+        \"\"\"BPR step: maximize margin between positive and negative items.\n+        X_pos and X_neg are same shape (B, F) representing paired pos/neg samples.\n+        \"\"\"\n+        B = len(X_pos)\n+        \n+        # Forward pass for positive and negative items\n+        z_pos, E_pos, S_pos = self.logits(X_pos)\n+        z_neg, E_neg, S_neg = self.logits(X_neg)\n+        \n+        # BPR loss: sigmoid(z_pos - z_neg) should be close to 1\n+        # Gradient w.r.t z_pos is positive, w.r.t z_neg is negative\n+        z_diff = z_pos - z_neg\n+        sigmoid_diff = sigmoid(z_diff)\n+        \n+        # Gradient of log-sigmoid(z_pos - z_neg): (1 - sigmoid(z_diff))\n+        g = (1.0 - sigmoid_diff) / B\n+        \n+        # Accumulate gradients for V and W\n         gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n-        np.add.at(gW, X, g[:, None])\n-        np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n-        gV += self.l2 * self.V; gW += self.l2 * self.W\n+        \n+        # Positive item gradients\n+        np.add.at(gW, X_pos, g[:, None])\n+        np.add.at(gV, X_pos, g[:, None, None] * (S_pos[:, None, :] - E_pos))\n+        \n+        # Negative item gradients (negative sign)\n+        np.add.at(gW, X_neg, -g[:, None])\n+        np.add.at(gV, X_neg, -g[:, None, None] * (S_neg[:, None, :] - E_neg))\n+        \n+        # L2 regularization\n+        gV += self.l2 * self.V\n+        gW += self.l2 * self.W\n+        \n+        # Adam optimizer\n         self.t += 1\n         b1, b2, eps = 0.9, 0.999, 1e-8\n         for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n             M *= b1; M += (1 - b1) * G\n             Vv *= b2; Vv += (1 - b2) * (G * G)\n             P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n+        \n+        # Bias gradient\n         self.b -= self.lr * g.sum()\n+        \n+        # Return loss for monitoring\n+        loss = -np.mean(np.log(sigmoid_diff + 1e-9))\n+        return float(loss)\n \n     def predict(self, X, bs=200_000):\n         return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n \n \n-ap = argparse.ArgumentParser()\n-ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n-a = ap.parse_args()\n+def create_bpr_pairs(X, y, rng, users=None):\n+    \"\"\"Create BPR pairs: for each positive, sample a negative from the same user.\n+    \n+    Args:\n+        X: (N, F) feature matrix\n+        y: (N,) labels\n+        rng: random generator\n+        users: (N,) user IDs, if provided will sample negatives only within same user\n+    \n+    Returns:\n+        (X_pos, X_neg) both shape (M, F) where M <= N (only positives are kept)\n+    \"\"\"\n+    \n+    if users is None:\n+        # Global BPR: for each positive, randomly sample a negative from all samples\n+        pos_idx = np.where(y == 1)[0]\n+        neg_idx = np.where(y == 0)[0]\n+        \n+        if len(pos_idx) == 0 or len(neg_idx) == 0:\n+            # Fallback: return empty pairs\n+            return np.empty((0, X.shape[1]), dtype=X.dtype), np.empty((0, X.shape[1]), dtype=X.dtype)\n+        \n+        # Sample with replacement\n+        sampled_neg_idx = rng.choice(neg_idx, size=len(pos_idx), replace=True)\n+        return X[pos_idx], X[sampled_neg_idx]\n+    else:\n+        # Within-user BPR: for each positive impression of a user, sample a negative from same user\n+        X_pos_list = []\n+        X_neg_list = []\n+        \n+        by_user = collections.defaultdict(lambda: {'pos': [], 'neg': []})\n+        for i in range(len(X)):\n+            u = users[i]\n+            if y[i] == 1:\n+                by_user[u]['pos'].append(i)\n+            else:\n+                by_user[u]['neg'].append(i)\n+        \n+        for u, indices in by_user.items():\n+            pos_idxs = indices['pos']\n+            neg_idxs = indices['neg']\n+            \n+            if len(pos_idxs) > 0 and len(neg_idxs) > 0:\n+                # Sample negatives with replacement for each positive\n+                sampled_neg_idxs = rng.choice(neg_idxs, size=len(pos_idxs), replace=True)\n+                X_pos_list.append(X[pos_idxs])\n+                X_neg_list.append(X[sampled_neg_idxs])\n+        \n+        if len(X_pos_list) == 0:\n+            return np.empty((0, X.shape[1]), dtype=X.dtype), np.empty((0, X.shape[1]), dtype=X.dtype)\n+        \n+        return np.vstack(X_pos_list), np.vstack(X_neg_list)\n \n-splits = load(a.data_dir)\n-enc, dim = encode(splits)\n-Xtr, ytr, _ = enc['train']\n-Xva, yva, uva = enc['valid']\n \n-m = FM(dim)\n-rng = np.random.default_rng(42)\n-best, best_state, bad = -1, None, 0\n-for ep in range(40):\n-    idx = rng.permutation(len(ytr))\n-    for i in range(0, len(idx), 8192):\n-        m.step(Xtr[idx[i:i + 8192]], ytr[idx[i:i + 8192]])\n-    va = evaluate(uva, yva, m.predict(Xva))\n-    if va['primary'] > best + 1e-5:\n-        best, bad = va['primary'], 0\n-        best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n-    else:\n-        bad += 1\n-        if bad >= 4:\n-            break\n-\n-m.V, m.W, m.b = best_state\n-result = evaluate(uva, yva, m.predict(Xva))\n-print(json.dumps({key: float(value) for key, value in result.items()}))\n+if __name__ == '__main__':\n+    ap = argparse.ArgumentParser()\n+    ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n+    a = ap.parse_args()\n+    \n+    splits = load(a.data_dir)\n+    enc, dim = encode(splits)\n+    \n+    Xtr, ytr, utr = enc['train']\n+    Xva, yva, uva = enc['valid']\n+    \n+    # Train BPR model\n+    m = FM(dim, k=16, lr=0.001, seed=42)\n+    rng = np.random.default_rng(42)\n+    \n+    best, best_state, bad = -1, None, 0\n+    \n+    for ep in range(1, 41):\n+        t0 = time.time()\n+        \n+        # Create BPR pairs within-user on the training set\n+        X_pos, X_neg = create_bpr_pairs(Xtr, ytr, rng, users=utr)\n+        \n+        if len(X_pos) == 0:\n+            continue\n+        \n+        # Shuffle pairs\n+        n_pairs = len(X_pos)\n+        idx = rng.permutation(n_pairs)\n+        X_pos = X_pos[idx]\n+        X_neg = X_neg[idx]\n+        \n+        # Mini-batch training\n+        losses = []\n+        for i in range(0, n_pairs, 8192):\n+            loss = m.step_bpr(X_pos[i:i + 8192], X_neg[i:i + 8192])\n+            losses.append(loss)\n+        \n+        # Validation\n+        va = evaluate(uva, yva, m.predict(Xva))\n+        \n+        print(f\"  epoch {ep:2d} | loss {np.mean(losses):.4f} | valid GAUC {va['GAUC']:.4f} \"\n+              f\"nDCG@5 {va['nDCG@5']:.4f} primary {va['primary']:.4f} | {time.time()-t0:.1f}s\")\n+        \n+        if va['primary'] > best + 1e-5:\n+            best, bad = va['primary'], 0\n+            best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+        else:\n+            bad += 1\n+            if bad >= 4:\n+                print(f\"  early stop at epoch {ep}\")\n+                break\n+    \n+    # Restore best state\n+    if best_state is not None:\n+        m.V, m.W, m.b = best_state\n+    \n+    scores_valid = m.predict(Xva)\n+    \n+    # Output\n+    print(json.dumps(evaluate(uva, yva, scores_valid)))", "status": "failed", "internal_repairs": 3, "metrics": null, "delta_vs_baseline": null, "pct_of_headroom": null, "is_new_best": false, "error": "REJECTED: test-set access detected in model.py. Violations: ['enc[\\'test\\'] access (pattern: enc\\\\s*\\\\[\\\\s*[\\'\"]test[\\'\"]\\\\s*\\\\])', 'subscript access to the test split']. Remove any access to splits['test'] or enc['test'] and retry.", "tokens": {"input": 105291, "output": 10027}, "wall_seconds": 70.68048310279846, "human_intervention": false}
