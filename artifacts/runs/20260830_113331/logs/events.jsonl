{"iteration": 0, "session_type": "builder", "timestamp": "2026-08-30T11:34:06.601362Z", "parent_iteration": null, "hypothesis": "FM baseline (k=16, pointwise BCE, Adam) — no change", "hypothesis_source": "harness_root", "target_component": "other", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_113331/trial_000/model.py", "code_diff": "--- baseline\n+++ trial_000/model.py\n@@ -0,0 +1,79 @@\n+\"\"\"Root candidate: FM baseline (k=16, pointwise BCE) adapted for harness contract.\"\"\"\n+import sys\n+sys.path.insert(0, r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/baseline_kuairand-starter-kit')\n+\n+import argparse, json, time, collections\n+import numpy as np\n+from data import load, encode, FIELDS\n+from evaluate import evaluate\n+\n+\n+def sigmoid(x):\n+    return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n+\n+\n+class FM:\n+    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=42):\n+        rng = np.random.default_rng(seed)\n+        self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n+        self.W = np.zeros(dim, dtype=np.float32)\n+        self.b = np.float32(0.0)\n+        self.lr, self.l2 = lr, l2\n+        self.mV = np.zeros_like(self.V); self.vV = np.zeros_like(self.V)\n+        self.mW = np.zeros_like(self.W); self.vW = np.zeros_like(self.W)\n+        self.t = 0\n+\n+    def logits(self, X):\n+        E = self.V[X]\n+        S = E.sum(1)\n+        inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n+        return self.b + self.W[X].sum(1) + inter, E, S\n+\n+    def step(self, X, y):\n+        B = len(y)\n+        z, E, S = self.logits(X)\n+        g = ((sigmoid(z) - y) / B).astype(np.float32)\n+        gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n+        np.add.at(gW, X, g[:, None])\n+        np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n+        gV += self.l2 * self.V; gW += self.l2 * self.W\n+        self.t += 1\n+        b1, b2, eps = 0.9, 0.999, 1e-8\n+        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n+            M *= b1; M += (1 - b1) * G\n+            Vv *= b2; Vv += (1 - b2) * (G * G)\n+            P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n+        self.b -= self.lr * g.sum()\n+\n+    def predict(self, X, bs=200_000):\n+        return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n+\n+\n+ap = argparse.ArgumentParser()\n+ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n+a = ap.parse_args()\n+\n+splits = load(a.data_dir)\n+enc, dim = encode(splits)\n+Xtr, ytr, _ = enc['train']\n+Xva, yva, uva = enc['valid']\n+\n+m = FM(dim)\n+rng = np.random.default_rng(42)\n+best, best_state, bad = -1, None, 0\n+for ep in range(40):\n+    idx = rng.permutation(len(ytr))\n+    for i in range(0, len(idx), 8192):\n+        m.step(Xtr[idx[i:i + 8192]], ytr[idx[i:i + 8192]])\n+    va = evaluate(uva, yva, m.predict(Xva))\n+    if va['primary'] > best + 1e-5:\n+        best, bad = va['primary'], 0\n+        best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+    else:\n+        bad += 1\n+        if bad >= 4:\n+            break\n+\n+m.V, m.W, m.b = best_state\n+result = evaluate(uva, yva, m.predict(Xva))\n+print(json.dumps({key: float(value) for key, value in result.items()}))", "status": "success", "internal_repairs": 0, "metrics": {"GAUC": 0.6674313545227051, "nDCG@5": 0.536278486251831, "primary": 0.6018549203872681, "users": 22377.0, "rows": 124909.0}, "delta_vs_baseline": 0.0002549203872680428, "pct_of_headroom": 0.10329027036792657, "is_new_best": true, "error": null, "tokens": {"input": 15540, "output": 1942}, "wall_seconds": 35.23585891723633, "human_intervention": false}
{"iteration": 1, "session_type": "builder", "timestamp": "2026-08-30T11:34:57.494825Z", "parent_iteration": 0, "hypothesis": "Implement BPR: Bayesian Personalized Ranking on top of the parent model and evaluate it. See the 'bpr' entry in the method corpus for the implementation details and the conditions under which it does not help.", "hypothesis_source": "corpus_seed_bpr", "target_component": "loss", "code_path": "/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/experiment_workspace/20260830_113331/trial_001/model.py", "code_diff": "--- trial_000/model.py\n+++ trial_001/model.py\n@@ -1,19 +1,18 @@\n-\"\"\"Root candidate: FM baseline (k=16, pointwise BCE) adapted for harness contract.\"\"\"\n import sys\n sys.path.insert(0, r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/baseline_kuairand-starter-kit')\n-\n-import argparse, json, time, collections\n-import numpy as np\n from data import load, encode, FIELDS\n from evaluate import evaluate\n-\n-\n-def sigmoid(x):\n+import json, argparse\n+\n+import numpy as np\n+import time\n+from collections import defaultdict\n+\n+def sigmoid(x): \n     return 1.0 / (1.0 + np.exp(-np.clip(x, -30, 30)))\n \n-\n class FM:\n-    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=42):\n+    def __init__(self, dim, k=16, lr=0.001, l2=1e-6, seed=0):\n         rng = np.random.default_rng(seed)\n         self.V = rng.normal(0, 0.01, (dim, k)).astype(np.float32)\n         self.W = np.zeros(dim, dtype=np.float32)\n@@ -24,15 +23,15 @@\n         self.t = 0\n \n     def logits(self, X):\n-        E = self.V[X]\n-        S = E.sum(1)\n+        E = self.V[X]                                   # (B,F,k)\n+        S = E.sum(1)                                    # (B,k)\n         inter = 0.5 * ((S ** 2).sum(1) - (E ** 2).sum((1, 2)))\n         return self.b + self.W[X].sum(1) + inter, E, S\n \n     def step(self, X, y):\n         B = len(y)\n         z, E, S = self.logits(X)\n-        g = ((sigmoid(z) - y) / B).astype(np.float32)\n+        g = ((sigmoid(z) - y) / B).astype(np.float32)    # (B,)\n         gV = np.zeros_like(self.V); gW = np.zeros_like(self.W)\n         np.add.at(gW, X, g[:, None])\n         np.add.at(gV, X, g[:, None, None] * (S[:, None, :] - E))\n@@ -44,36 +43,173 @@\n             Vv *= b2; Vv += (1 - b2) * (G * G)\n             P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n         self.b -= self.lr * g.sum()\n+        return float(-np.mean(y * np.log(sigmoid(z) + 1e-9) + (1 - y) * np.log(1 - sigmoid(z) + 1e-9)))\n+\n+    def step_bpr(self, X_pos, X_neg):\n+        \"\"\"BPR step: X_pos and X_neg are (B, F) arrays of indices.\n+        Loss = -log(sigmoid(score(pos) - score(neg)))\"\"\"\n+        B = len(X_pos)\n+        z_pos, E_pos, S_pos = self.logits(X_pos)  # (B,), (B,F,k), (B,k)\n+        z_neg, E_neg, S_neg = self.logits(X_neg)  # (B,), (B,F,k), (B,k)\n+        \n+        # Pairwise difference\n+        diff = z_pos - z_neg  # (B,)\n+        s_diff = sigmoid(-diff)  # sigmoid(-(pos-neg)) = 1/(1+exp(pos-neg))\n+        \n+        # Gradient w.r.t. the difference: d(-log(sigmoid(x)))/dx = -sigmoid(-x) for x = pos-neg\n+        g_diff = -s_diff / B  # (B,)\n+        \n+        # For pos: gradient is positive (want to increase score)\n+        # For neg: gradient is negative (want to decrease score)\n+        gV = np.zeros_like(self.V)\n+        gW = np.zeros_like(self.W)\n+        \n+        # Accumulate gradients for positive samples\n+        np.add.at(gW, X_pos, g_diff[:, None])\n+        np.add.at(gV, X_pos, g_diff[:, None, None] * (S_pos[:, None, :] - E_pos))\n+        \n+        # Accumulate gradients for negative samples\n+        np.add.at(gW, X_neg, -g_diff[:, None])\n+        np.add.at(gV, X_neg, -g_diff[:, None, None] * (S_neg[:, None, :] - E_neg))\n+        \n+        # L2 regularization\n+        gV += self.l2 * self.V\n+        gW += self.l2 * self.W\n+        \n+        # Adam update\n+        self.t += 1\n+        b1, b2, eps = 0.9, 0.999, 1e-8\n+        for P, G, M, Vv in ((self.V, gV, self.mV, self.vV), (self.W, gW, self.mW, self.vW)):\n+            M *= b1; M += (1 - b1) * G\n+            Vv *= b2; Vv += (1 - b2) * (G * G)\n+            P -= self.lr * (M / (1 - b1 ** self.t)) / (np.sqrt(Vv / (1 - b2 ** self.t)) + eps)\n+        self.b -= self.lr * g_diff.sum()\n+        \n+        # Return BPR loss: -log(sigmoid(pos - neg))\n+        return float(-np.mean(np.log(sigmoid(diff) + 1e-9)))\n \n     def predict(self, X, bs=200_000):\n         return np.concatenate([self.logits(X[i:i + bs])[0] for i in range(0, len(X), bs)])\n \n-\n-ap = argparse.ArgumentParser()\n-ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n-a = ap.parse_args()\n-\n-splits = load(a.data_dir)\n-enc, dim = encode(splits)\n-Xtr, ytr, _ = enc['train']\n-Xva, yva, uva = enc['valid']\n-\n-m = FM(dim)\n-rng = np.random.default_rng(42)\n-best, best_state, bad = -1, None, 0\n-for ep in range(40):\n-    idx = rng.permutation(len(ytr))\n-    for i in range(0, len(idx), 8192):\n-        m.step(Xtr[idx[i:i + 8192]], ytr[idx[i:i + 8192]])\n-    va = evaluate(uva, yva, m.predict(Xva))\n-    if va['primary'] > best + 1e-5:\n-        best, bad = va['primary'], 0\n-        best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+def create_bpr_pairs(X, y, users, seed=42):\n+    \"\"\"Create BPR pairs: for each user, sample positive-negative pairs.\n+    Returns: (X_pos, X_neg) arrays ready for training.\"\"\"\n+    rng = np.random.default_rng(seed)\n+    \n+    # Group by user\n+    user_to_indices = defaultdict(list)\n+    for idx, user_id in enumerate(users):\n+        user_to_indices[user_id].append(idx)\n+    \n+    X_pos_list = []\n+    X_neg_list = []\n+    \n+    for user_id, indices in user_to_indices.items():\n+        indices = np.array(indices, dtype=np.int32)\n+        y_user = y[indices]\n+        \n+        # Find positive and negative items for this user\n+        pos_mask = y_user == 1\n+        neg_mask = y_user == 0\n+        \n+        pos_indices = indices[pos_mask]\n+        neg_indices = indices[neg_mask]\n+        \n+        # Skip if no positives or no negatives\n+        if len(pos_indices) == 0 or len(neg_indices) == 0:\n+            continue\n+        \n+        # Sample pairs: for each positive, sample a negative\n+        n_pairs = len(pos_indices)\n+        sampled_pos = pos_indices  # Use all positives\n+        sampled_neg = rng.choice(neg_indices, size=n_pairs, replace=True)\n+        \n+        X_pos_list.append(X[sampled_pos])\n+        X_neg_list.append(X[sampled_neg])\n+    \n+    if not X_pos_list:\n+        return None, None\n+    \n+    X_pos = np.vstack(X_pos_list)\n+    X_neg = np.vstack(X_neg_list)\n+    \n+    return X_pos, X_neg\n+\n+def run_fm_bpr(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, seed=0, verbose=True):\n+    enc, dim = encode(splits)\n+    Xtr, ytr, utr = enc['train']\n+    Xva, yva, uva = enc['valid']\n+    \n+    m = FM(dim, k=k, lr=lr, seed=seed)\n+    rng = np.random.default_rng(seed)\n+    best, best_state, bad = -1, None, 0\n+    \n+    # Create BPR pairs once before training\n+    X_pos, X_neg = create_bpr_pairs(Xtr, ytr, utr, seed=seed)\n+    \n+    if X_pos is None:\n+        if verbose:\n+            print(\"Warning: No BPR pairs could be created. Falling back to pointwise training.\")\n+        # Fallback to pointwise training\n+        for ep in range(1, epochs + 1):\n+            idx = rng.permutation(len(ytr))\n+            t0 = time.time()\n+            losses = [m.step(Xtr[idx[i:i + bs]], ytr[idx[i:i + bs]]) for i in range(0, len(idx), bs)]\n+            va = evaluate(uva, yva, m.predict(Xva))\n+            if verbose:\n+                print(f\"  epoch {ep:2d} | loss {np.mean(losses):.4f} | valid GAUC {va['GAUC']:.4f} \"\n+                      f\"nDCG@5 {va['nDCG@5']:.4f} primary {va['primary']:.4f} | {time.time()-t0:.1f}s\")\n+            if va['primary'] > best + 1e-5:\n+                best, bad = va['primary'], 0\n+                best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+            else:\n+                bad += 1\n+                if bad >= patience:\n+                    if verbose:\n+                        print(f\"  early stop at epoch {ep}\")\n+                    break\n     else:\n-        bad += 1\n-        if bad >= 4:\n-            break\n-\n-m.V, m.W, m.b = best_state\n-result = evaluate(uva, yva, m.predict(Xva))\n-print(json.dumps({key: float(value) for key, value in result.items()}))\n+        if verbose:\n+            print(f\"Created {len(X_pos)} BPR pairs from {len(ytr)} training samples\")\n+        \n+        n_pairs = len(X_pos)\n+        for ep in range(1, epochs + 1):\n+            idx = rng.permutation(n_pairs)\n+            t0 = time.time()\n+            losses = []\n+            for i in range(0, len(idx), bs):\n+                batch_idx = idx[i:i + bs]\n+                loss = m.step_bpr(X_pos[batch_idx], X_neg[batch_idx])\n+                losses.append(loss)\n+            \n+            va = evaluate(uva, yva, m.predict(Xva))\n+            if verbose:\n+                print(f\"  epoch {ep:2d} | loss {np.mean(losses):.4f} | valid GAUC {va['GAUC']:.4f} \"\n+                      f\"nDCG@5 {va['nDCG@5']:.4f} primary {va['primary']:.4f} | {time.time()-t0:.1f}s\")\n+            \n+            if va['primary'] > best + 1e-5:\n+                best, bad = va['primary'], 0\n+                best_state = (m.V.copy(), m.W.copy(), np.float32(m.b))\n+            else:\n+                bad += 1\n+                if bad >= patience:\n+                    if verbose:\n+                        print(f\"  early stop at epoch {ep}\")\n+                    break\n+    \n+    m.V, m.W, m.b = best_state\n+    return evaluate(uva, yva, m.predict(Xva))\n+\n+if __name__ == '__main__':\n+    ap = argparse.ArgumentParser()\n+    ap.add_argument('--data_dir', default=r'/Users/bviveka/Desktop/tiktok/TikTokTechJam_STierCo/datasets/KuaiRand-Pure/data')\n+    a = ap.parse_args()\n+    \n+    print(f\"loading {a.data_dir} ...\")\n+    splits = load(a.data_dir)\n+    print({k_: len(v) for k_, v in splits.items()}, f\"fields={FIELDS}\")\n+    \n+    print(\"\\n=== BPR (seed=42) ===\")\n+    result = run_fm_bpr(splits, k=16, lr=0.001, epochs=40, bs=8192, patience=4, seed=42, verbose=True)\n+    \n+    print(json.dumps({k: float(v) for k, v in result.items()}))", "status": "failed", "internal_repairs": 0, "metrics": null, "delta_vs_baseline": null, "pct_of_headroom": null, "is_new_best": false, "error": null, "tokens": {"input": 64354, "output": 4616}, "wall_seconds": 50.87851881980896, "human_intervention": false}
